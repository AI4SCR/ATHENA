# Install ATHENA

```{code-block}
# assuming you have a ssh key set up on GitHub

# install spatialOmics data container
pip install "git@github.ibm.com/art/spatial-omics.git@master"

# install ATHENA package
pip install "git@github.ibm.com/art/spatial-heterogeneity.git@master"
```